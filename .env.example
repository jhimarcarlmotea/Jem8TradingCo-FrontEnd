# Frontend environment variables for Vite (copy to `.env` or set in CI/CD)
# Base URL for API (include trailing /api if your backend serves API under /api)
VITE_API_BASE_URL=https://your-api-env.elasticbeanstalk.com/api

# App metadata
VITE_APP_TITLE=Jem8TradingCo

# Dev helpers
VITE_DISABLE_DEV_FEATURES=false
